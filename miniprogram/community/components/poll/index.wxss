@import '../../styles.wxss';
.studio.poll-panel {
  min-height: 0;
  padding: 0;
  background: transparent;
}
.bar-track {
  height: 8rpx;
  background: var(--cedh-line);
  margin-top: 16rpx;
  border-radius: 8rpx;
  overflow: hidden;
}
.bar-fill {
  background: var(--cedh-accent);
  height: 100%;
}
