page {
  height: 100%;
  overflow: hidden;
  background: #D0F03C;
}

.home {
  position: relative; /* 背景线场按它定位，不能依赖默认的初始包含块 */
  box-sizing: border-box;
  width: 100%;
  height: 100vh;
  min-height: 100vh;
  padding: 0;
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
  overflow: hidden;
  color: #0A0A0A;
  background: #D0F03C;
  font-family: "HomePixel", "Fusion Pixel 10px Monospaced SC", "Zpix", ui-monospace, monospace;
}

/* ===== 背景：整页 Voronoi 线场 =====
   选它是因为首页的约束把别的路都堵死了：border-radius 锁死为唯一一条 0，圆形画不了；
   渐变、背景图、裁切路径也全在禁用之列（测试连注释一起扫，所以这里只能中文描述）。
   只剩「直线段 + transform + 既有色板」，而 Voronoi 的胞元边恰好全是直线段，
   又天然不规则、没有可重复的单元。线段坐标由 scripts/build-home-voronoi.js 构建期算好。

   线场铺满整页，压在注释墙、字标与八行目录之下作底纹。线段的 left / top / width
   一律用 vw：三者同单位，几何就与容器尺寸和屏幕长宽比彻底解耦。若改回百分比，
   横向按容器宽解析、纵向按容器高解析，容器一旦不是正方形，线段就落不到该落的端点上，
   胞元会在交汇处裂开。构建期画布高 240vw（覆盖到 2.4 的屏幕长宽比），
   四周另留外扩量，旋转扫到边角时不会露白；溢出部分由 .home 的 overflow:hidden 裁掉，
   读起来像「更大结构的一角」而不是一张完整图案。 */
.home-field {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0.45;
  pointer-events: none;
  will-change: transform;
  animation: home-field-turn 71s ease-in-out infinite alternate;
}

.home-field-drift {
  width: 100%;
  height: 100%;
  will-change: transform;
  animation: home-field-drift 53s linear infinite;
}

.home-field-line {
  position: absolute;
  height: 1px;
  background: rgba(0,0,0,0.35);
  transform-origin: left center;
}

/* 两层动效叠加出不规律，都不碰数据层：真正的 Voronoi 动画（种子移动、胞元重算）
   要每帧重算七十多条线再 setData 过桥，是移动端最贵的做法；这里两个 transform 走合成器，
   线段再多也不额外付代价。

   主运动是 drift 这条封闭回路，53s linear 无限循环。三处刻意的选择：
   · 不用 alternate。往返式动画在两端速度归零，而开屏那一刻正好停在起点，
     用户最可能盯着看的头十几秒恰恰是最慢的一段——上一版就是这么「看着没动」的
     （71s ease-in-out ±2.4°，开屏 15 秒内页面中心只移动约 0.7px，肉眼根本读不出来）。
     闭合回路从第一帧起就是匀速的，且永不折返。
   · 用 linear 而非 ease。缓动会在每个关键帧附近造出减速区，同样是看不见的死区。
   · 回路本身是不规则多边形，六个关键帧的时间间距也不等（14/15/18/14/22/17），
     段速在 0.41–0.79 vw/s 之间差近两倍，因此不会读成一个匀速转圈。
   周长 32.1vw / 53s ≈ 0.6vw/s，在 390 宽的机型上约 2.4px/s、十秒走 24px：
   看得出在动，又不会抢注意力。

   turn 那层是次要的差分运动：绕整页中心慢转 ±2.4°，让上下不是整块平移的死板感。
   它用 alternate（实际周期 142s），与 drift 的 53s 互质，合成轨迹约 2.1 小时才回到
   同一相位——一次使用里永远走不完一轮。
   离中心最远的页角约 119vw，2.4° 下位移约 5vw，加上 drift 的 5.6vw 仍远小于构建期
   留的 26vw 外扩量，扫到边角不会露白。位移量用 vw，与线段坐标同单位，幅度不随屏高变化。 */
@keyframes home-field-turn {
  from { transform: rotate(-2.4deg); }
  to { transform: rotate(2.4deg); }
}

@keyframes home-field-drift {
  0% { transform: translate3d(5.2vw, -1.4vw, 0); }
  14% { transform: translate3d(2.1vw, 3.6vw, 0); }
  29% { transform: translate3d(-3.4vw, 5.1vw, 0); }
  47% { transform: translate3d(-5.6vw, 0.8vw, 0); }
  61% { transform: translate3d(-2.8vw, -4.2vw, 0); }
  83% { transform: translate3d(1.9vw, -5.3vw, 0); }
  100% { transform: translate3d(5.2vw, -1.4vw, 0); }
}

.home-content {
  position: relative;
  display: flex;
  width: 100%;
  height: 100%;
  min-height: 0;
  flex-direction: column;
}

.home-hero {
  box-sizing: border-box;
  display: flex;
  min-height: 0;
  flex: 0 0 45vh;
  flex-direction: column;
  padding-right: 24rpx;
  padding-bottom: 30rpx;
  padding-left: 20rpx;
  overflow: hidden;
}

/* 规则宣言：满墙涂鸦式压迫感——极密行距、整体轻微倾斜、贴边溢出，不再优雅留白 */
.home-format-notes {
  display: flex;
  flex: 0 0 auto;
  flex-direction: column;
  align-items: flex-start;
  margin-right: -12rpx;
  transform: rotate(-1.3deg);
  transform-origin: left top;
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
}

.home-format-note {
  display: block;
  max-width: 100%;
  color: rgba(0,0,0,0.35);
  font-size: 7px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.4px;
  line-height: 1.15;
  text-transform: uppercase;
  white-space: nowrap;
}

.home-format-note-next {
  margin-top: 3rpx;
}

.home-format-para {
  margin-top: 12rpx;
  color: #0A0A0A;
}

/* 审查/遮挡：每次进页随机涂黑一整行——把那一行自己的文字盒染成实心黑、字色同底而隐去。
   黑条即文字盒本身，故与文字天然等宽等高、必定盖住；不做像素定位，字体回退与屏高断点
   都不会让它悬空。左右各外扩 6rpx 作马克笔涂抹感，用负 margin 抵消保持左缘对齐。 */
.home-format-note.is-redacted {
  padding: 0 6rpx;
  margin-left: -6rpx;
  color: #0A0A0A;
  background: #0A0A0A;
}

.home-title-block {
  display: flex;
  min-width: 0;
  margin-top: auto;
  flex-direction: column;
  align-items: flex-start;
  overflow: visible;
}

.home-title-brand,
.home-title-zh {
  display: block;
  font-style: normal;
  font-weight: 400;
  white-space: nowrap;
}

/* 喊叫：超粗 grotesk 横向拉伸变形 + 倾斜；字号留边以保证品牌名在窄机型不被右缘裁掉 */
.home-title-brand {
  color: #FFFFFF;
  font-family: "cEDHDisplay", "Arial Black", Impact, "Avenir Next Condensed", "DIN Condensed", sans-serif;
  font-size: 48px;
  font-weight: 900;
  letter-spacing: -0.06em;
  line-height: 0.84;
  -webkit-font-smoothing: antialiased;
  -webkit-text-stroke: 0.6px #FFFFFF;
  transform: scaleX(1.18) skewX(-10deg);
  transform-origin: left center;
}

.home-title-zh {
  margin-top: 20rpx;
  color: #FFFFFF;
  font-size: 18px;
  letter-spacing: 5px;
  line-height: 1;
}

.home-actions {
  box-sizing: border-box;
  display: flex;
  min-height: 0;
  flex: 1 1 55%;
  flex-direction: column;
  border-top: 5px solid #0A0A0A;
}

/* 有意破坏：行高不等、部分偏移打破左对齐线、编号巨号压字、分割线粗暴混用 */
.home-button {
  box-sizing: border-box;
  display: flex;
  min-width: 0;
  min-height: 0;
  flex: 1 1 0;
  align-items: center;
  padding: 0 40rpx 0 20rpx;
  overflow: visible;
  color: #0A0A0A;
  background: transparent;
  border: 0;
  border-bottom: 1px solid rgba(0,0,0,0.35);
  border-radius: 0;
  transition: color 60ms linear, background-color 60ms linear;
}

.home-button-tall {
  flex-grow: 1.45;
}

.home-button-short {
  flex-grow: 0.72;
}

.home-button-shift {
  padding-left: 64rpx;
}

.home-button-heavy {
  border-bottom: 5px solid #0A0A0A;
}

.home-button-index {
  display: block;
  flex: 0 0 auto;
  min-width: 84rpx;
  color: #0A0A0A;
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
  font-size: 16px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  letter-spacing: 0;
  line-height: 0.8;
}

.home-button-index-xl {
  font-size: 28px;
  min-width: 96rpx;
}

.home-button-index-xxl {
  font-size: 38px;
  min-width: 108rpx;
}

/* 偏移行的编号负位移，压到左缘外并叠上文字 */
.home-button-index-over {
  font-size: 24px;
  min-width: 56rpx;
  margin-left: -40rpx;
}

/* ===== 编号的字面：八行八种字体 × 八种赛博朋克处置 =====
   编号仍是一枚完整的两位数，**同一枚编号内两位字号完全一致**，不做缩放、旋转与位移——
   八行之间的差别只来自「字体 + 投影」这两样。

   投影是这一版唯一新增的手段，也只开放给编号（ui.suite 校验：全页别处仍禁 text-shadow）。
   颜色一律取自既有六色板：电光蓝 #00B3FF 与纯黑 #0A0A0A 打在酸性黄绿底上，本身就是
   霓虹管 + 硬阴影的配色，不需要再引入紫红。八种处置各取一种赛博语汇：
   立体挤出 / 霓虹辉光 / 色差错位 / 空心描边 / CRT 泛光 / 残影拖尾 / 扫描线错行 / 硬投影。

   两条纪律：
   · 偏移一律 ≤4px、模糊 ≤14px——投影不参与布局，但视觉上会越界撞到右侧中文；
   · 反相态（glitch 行 / 按压）统一收掉投影：那两种状态下底色正是影色，留着会糊成一团。

   衬线在这一版被整体换掉了：Georgia / Times 这类读起来是编辑部而不是赛博，
   八行现在只在超粗 / 窄体 / 等宽 / 点阵 / 细体之间分配。安卓系统字体少，
   落到同一通用族的行靠「字重 + 投影」再分开，测试按这个组合键校验。

   字号一律由行的档位类（-xl / -xxl / -over）给，字面类不碰 font-size——
   唯一例外是点阵那行，点阵必须落在 10px 网格整数倍上才锐利，该行档位 38px 会糊，取整 40px。 */

/* 01 人格测试：超粗 + 立体挤出。三层硬投影逐级推出，末层落电光蓝，像印歪的丝网海报 */
.home-index-face-01 {
  font-family: "cEDHDisplay", "Arial Black", Impact, "Helvetica Neue", sans-serif;
  font-weight: 900;
  letter-spacing: -0.04em;
  text-shadow: 1px 1px 0 #0A0A0A, 2px 2px 0 #0A0A0A, 3px 3px 0 #00B3FF;
}

/* 02 火花觉醒：窄体 + 霓虹辉光。两层同色模糊叠出灯管的散射，呼应「觉醒」那点亮起来的意思 */
.home-index-face-02 {
  font-family: "Avenir Next Condensed", "DIN Condensed", Haettenschweiler, "Arial Narrow", "sans-serif-condensed", sans-serif;
  font-weight: 700;
  letter-spacing: 0.02em;
  text-shadow: 0 0 6px #00B3FF, 0 0 14px #00B3FF;
}

/* 03 强度分级：打字机等宽 + 色差错位。左黑右蓝反向硬分离，就是通道没对齐的那种故障，
   配这行的分析属性——数据读出来了，但信号是脏的 */
.home-index-face-03 {
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-shadow: 2px 0 0 #00B3FF, -2px 0 0 #0A0A0A;
}

/* 04 套牌试玩：无衬线特粗 + 空心描边。字身填成底色、四向 1px 硬投影描出轮廓，
   得到一枚镂空的数字。这是 -webkit-text-stroke 的替代做法——真机不支持描边时，
   text-stroke 会连同 color: transparent 一起把数字整枚吞掉，四向投影则不会静默失败 */
.home-index-face-04 {
  font-family: Impact, Haettenschweiler, "Arial Black", "Franklin Gothic Heavy", sans-serif;
  font-weight: 900;
  letter-spacing: 0.02em;
  color: #D0F03C;
  text-shadow: -1px -1px 0 #0A0A0A, 1px -1px 0 #0A0A0A, -1px 1px 0 #0A0A0A, 1px 1px 0 #0A0A0A;
}

/* 05 血量记录：点阵 + CRT 泛光。贴身一层黑收住字形，外面罩一圈大范围蓝晕，
   像老显像管上烧出来的读数。40px 落在 10px 网格整数倍上，放大后依然锐利 */
.home-index-face-05 {
  font-family: "HomePixel", "Fusion Pixel 10px Monospaced SC", "Zpix", ui-monospace, monospace;
  font-size: 40px;
  font-weight: 400;
  letter-spacing: 0.08em;
  text-shadow: 0 0 1px #0A0A0A, 0 0 12px #00B3FF;
}

/* 06 混沌工具：现代等宽 + 残影拖尾。同向三级递减重影，像画面没刷干净拖出来的尾巴 */
.home-index-face-06 {
  font-family: Menlo, Consolas, "Roboto Mono", "Droid Sans Mono", ui-monospace, monospace;
  font-weight: 400;
  letter-spacing: 0.06em;
  text-shadow: 2px 0 0 #0A0A0A, 4px 0 0 #00B3FF, 6px 0 8px rgba(0,0,0,0.35);
}

/* 07 我的主将：细体 + 扫描线错行。上下两侧各推 2px 且异色，字身像被隔行扫描切开 */
.home-index-face-07 {
  font-family: "Helvetica Neue", "PingFang SC", Roboto, "sans-serif-thin", sans-serif;
  font-weight: 200;
  letter-spacing: 0.06em;
  text-shadow: 0 -2px 0 #00B3FF, 0 2px 0 #0A0A0A;
}

/* 08 环境梯度：几何无衬线 + 单层硬投影。最干脆的一枚，海报式的实心偏移，不模糊不分色 */
.home-index-face-08 {
  font-family: Futura, "Century Gothic", "Trebuchet MS", "Questrial", sans-serif;
  font-weight: 400;
  letter-spacing: 0.08em;
  text-shadow: 4px 4px 0 #00B3FF;
}

.home-button-text {
  display: flex;
  min-width: 0;
  flex: 1 1 auto;
  align-items: baseline;
  justify-content: space-between;
  overflow: hidden;
  white-space: nowrap;
}

.home-button-zh {
  display: block;
  flex: 0 0 auto;
  color: #0A0A0A;
  font-size: 20px;
  font-weight: 400;
  letter-spacing: 3px;
  line-height: 1;
}

.home-button-zh-sm {
  font-size: 16px;
  letter-spacing: 2px;
}

.home-button-en {
  display: block;
  min-width: 0;
  margin-left: 20rpx;
  flex: 0 1 auto;
  overflow: hidden;
  /* 副标题是这一行里唯一说明「点进去会发生什么」的功能文本，不按装饰文本的标准压小压淡：
     10px + 0.7 alpha 在 #D0F03C 上约 7.5:1，过 WCAG AA 正文 4.5:1（原 9px / 0.55 只有约 4.46:1） */
  color: rgba(0,0,0,0.7);
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 1.5px;
  line-height: 1;
  text-overflow: ellipsis;
  white-space: nowrap;
  text-transform: uppercase;
}

/* NEW 角标：用事故色电光蓝——全页唯一的第五色，只在 glitch 行和按压反相时出现，
   因此它一现身就是全页最扎眼的东西，不需要动效也够抢眼。
   斜切 -12deg 呼应被 skewX 拉变形的 cEDH Tutor 字标，不是随便贴一个圆角标签。 */
.home-button-flag {
  flex: 0 0 auto;
  margin-left: 16rpx;
  padding: 3px 8px;
  color: #0A0A0A;
  background: #00B3FF;
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 1.5px;
  line-height: 1.2;
  transform: skewX(-12deg);
  white-space: nowrap;
}

/* 该行被随机 glitch 命中时整行已是电光蓝底，角标必须反相，
   否则蓝底蓝块直接糊成一片（与涂黑编号同一类坑） */
.home-button.is-glitch .home-button-flag {
  color: #00B3FF;
  background: #0A0A0A;
}

/* 粗暴分割：字符 /// 一整行当分隔线 */
.home-rule-slash {
  flex: 0 0 auto;
  padding: 1rpx 20rpx;
  overflow: hidden;
  color: #0A0A0A;
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 1px;
  line-height: 1;
  white-space: nowrap;
}

/* 事故色：随机一行"故障"成电光蓝底黑字，像一个 glitch/错误 */
.home-button.is-glitch {
  background: #00B3FF;
}

.home-button.is-glitch .home-button-index,
.home-button.is-glitch .home-button-zh,
.home-button.is-glitch .home-button-en {
  color: #0A0A0A;
}

/* 反相态统一收掉编号的投影：glitch 行底色正是电光蓝、按压态底色正是纯黑，
   而八种赛博处置的影色只有这两种，留着必然与底色同源、糊成一团。
   04 那枚空心数字的字身填的是页面底色，反相后会陷进新底色里整枚消失，须单独填回实心。 */
.home-button.is-glitch .home-button-index,
.home-index-active .home-button-index {
  text-shadow: none;
}

.home-button.is-glitch .home-index-face-04 {
  color: #0A0A0A;
}

.home-index-active .home-index-face-04 {
  color: #00B3FF;
}

/* 暴力按压：整块反相为黑底 + 电光蓝字，而非温柔缩放 */
.home-index-active {
  background: #0A0A0A;
}

.home-index-active .home-button-index,
.home-index-active .home-button-zh,
.home-index-active .home-button-en {
  color: #00B3FF;
}


/* 版权：极小近乎隐形的 fine print；左右加大内缩 + 安全区，避免贴边被屏幕圆角吞掉 */
.home-colophon {
  box-sizing: border-box;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  flex: 0 0 auto;
  align-items: baseline;
  padding: 10rpx 48rpx 12rpx;
  padding-right: calc(48rpx + constant(safe-area-inset-right));
  padding-right: calc(48rpx + env(safe-area-inset-right));
  padding-left: calc(48rpx + constant(safe-area-inset-left));
  padding-left: calc(48rpx + env(safe-area-inset-left));
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
}

.home-colophon-item {
  display: block;
  color: rgba(0,0,0,0.35);
  font-size: 6px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.5px;
  line-height: 1.3;
  text-transform: uppercase;
  white-space: nowrap;
}

.home-colophon-center {
  text-align: center;
}

.home-colophon-right {
  text-align: right;
}

/* 侧栏格言：竖排贴右侧脊；离右缘留出安全区、上下留白，避免被屏幕圆角/刘海吞掉 */
.home-edge-tagline {
  position: fixed;
  top: 0;
  bottom: 0;
  right: 0;
  display: flex;
  align-items: center;
  padding-top: 60rpx;
  padding-bottom: 60rpx;
  padding-right: 14rpx;
  padding-right: calc(14rpx + constant(safe-area-inset-right));
  padding-right: calc(14rpx + env(safe-area-inset-right));
  writing-mode: vertical-rl;
  text-orientation: sideways;
  color: rgba(0,0,0,0.35);
  font-family: "Courier New", Courier, "Nimbus Mono PS", ui-monospace, monospace;
  font-size: 8px;
  font-weight: 700;
  letter-spacing: 1px;
  line-height: 1;
  text-transform: uppercase;
  white-space: nowrap;
}

@media (max-width: 360px) {
  .home-title-brand {
    font-size: 42px;
  }

  .home-title-zh {
    font-size: 16px;
  }

  /* 窄屏只收紧间距，不再把功能文本降到 8px——挤版面该让装饰让路，不该让说明让路 */
  .home-button-en {
    margin-left: 12rpx;
    letter-spacing: 1px;
  }
}

@media (max-height: 700px) {
  .home-hero {
    flex-basis: 40vh;
    padding-bottom: 22rpx;
  }

  .home-format-note-next {
    margin-top: 2rpx;
  }

  .home-format-para {
    margin-top: 8rpx;
  }

  .home-title-brand {
    font-size: 42px;
  }

  .home-title-zh {
    margin-top: 14rpx;
    font-size: 15px;
  }
}

@media (max-height: 600px) {
  .home-hero {
    flex-basis: 40vh;
  }

  .home-title-brand {
    font-size: 38px;
  }

  .home-title-zh {
    margin-top: 10rpx;
    font-size: 13px;
  }

  .home-format-note {
    font-size: 6px;
    letter-spacing: 0.2px;
    line-height: 1.1;
  }

  .home-colophon-item {
    font-size: 6px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .home-button {
    transition: none;
  }

  /* 线场停在中位，图形本身仍在，只是不再漂移 */
  .home-field,
  .home-field-drift {
    animation: none;
  }
}
